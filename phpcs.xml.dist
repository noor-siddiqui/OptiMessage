<?xml version="1.0"?>
<ruleset name="Opti Message Ruleset">
    <description>Custom ruleset for Opti Message plugin.</description>

    <!-- Only scan PHP files. -->
    <arg name="extensions" value="php"/>

    <!-- Check up to 10 files simultaneously. -->
    <arg name="parallel" value="10"/>

    <!-- Show progress and use colors -->
    <arg value="ps"/>
    
    <!-- Hide all warnings -->
    <arg value="n"/>

    <file>.</file>

    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/assets/*</exclude-pattern>
    <exclude-pattern>*/languages/*</exclude-pattern>
    <exclude-pattern>*/plugin-update-checker/*</exclude-pattern>

    <rule ref="WordPress-Core">
        <exclude name="WordPress.DB.PreparedSQL.InterpolatedNotPrepared"/>
    </rule>
    <rule ref="WordPress-Docs" />
    <rule ref="WordPress-Extra" />

    <config name="testVersion" value="8.1-"/>

    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="optimessage"/>
            </property>
        </properties>
    </rule>

        <rule ref="WordPress.WP.Capabilities">
        <properties>
            <property name="custom_capabilities" type="array">
                <element value="manage_woocommerce"/>
            </property>
        </properties>
    </rule>

</ruleset>